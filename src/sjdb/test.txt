Project(
        Select(
                Select(
                        Select(
                                Select(
                                        Product(
                                                Product(Scan(A), Scan(B)),
                                                Scan(C)),
                                        Predicate(Atr(C2), ct.)),
                                Predicate(Atr(B2), Atr(C1))),
                        Predicate(Atr(A1), Atr(B1))),
                Predicate(Atr(A2), ct.)),
        Atr(A3))

Project(
        Select(
                Product(
                        Select(
                                Product(
                                        Select( Scan(A),
                                                Predicate(Atr(A2) , ct.)),
                                        Scan(B)),
                                Predicate( Atr(A1), Atr(B1))),
                        Select( Scan(C),
                                Predicate(Atr(C2), ct.))),
                Predicate(Atr(B2), Atr(C1))),
        Atr(A3))